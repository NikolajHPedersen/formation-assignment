# Run The Project

From the repository root, use Python 3.13 to install the needed package and run the script:

```bash
python3.13 -m venv .venv
source .venv/bin/activate
python3.13 -m pip install ortools
python3.13 src/main.py
```
